name: Build CV
on:
  push:
    branches: [main]
    paths: ['cv/**.tex', '.github/workflows/build-cv.yml']
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Compilar LaTeX (es + en)
        uses: xu-cheng/latex-action@v3
        with:
          working_directory: cv
          root_file: |
            cv.tex
            cv_en.tex
      - name: Publicar PDFs
        run: |
          mkdir -p resources
          cp cv/cv.pdf resources/cv.pdf
          cp cv/cv_en.pdf resources/cv_en.pdf
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add resources/cv.pdf resources/cv_en.pdf
          git diff --staged --quiet || git commit -m "Actualizar CV [skip ci]"
          git push
